spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/equivalencia_disciplina
    username: root
    password: equivalencia_disciplina
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
        hibernate:
          format_sql: false

server:
  port: 8080
